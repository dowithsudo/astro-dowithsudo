---
import AuthorCard from "@/components/AuthorCard.astro";
import Base from "@/layouts/Base.astro";
import { getListPage, getSinglePage } from "@/lib/contentParser.astro";
import PageHeader from "@/partials/PageHeader.astro";

// 1. Ambil teks halaman index dari folder 'id'
const authorIndex = await getListPage("authors", "id/-index");

if (!authorIndex || authorIndex.data.draft) {
  return Astro.redirect("/404");
}

// 2. Ambil semua data penulis
const allAuthors = await getSinglePage("authors");

// 3. Filter agar hanya penulis di folder 'id' yang muncul
const authors = allAuthors.filter((author) => author.id.startsWith("id/"));
---

<Base title={authorIndex.data.title}>
  <PageHeader title={authorIndex.data.title} />
  <section class="section-sm pb-0">
    <div class="container">
      <div class="row justify-center">
        {
          authors.map((author) => (
            <div class="mb-14 md:col-6 lg:col-4">
              <AuthorCard data={author} />
            </div>
          ))
        }
      </div>
    </div>
  </section>
</Base>